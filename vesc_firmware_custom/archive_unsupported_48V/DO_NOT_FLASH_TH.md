# ห้ามแฟลชไฟล์ในโฟลเดอร์นี้

บอร์ดได้รับการยืนยันว่าใช้ VSUPPLY nominal 24 V แล้ว ไฟล์ 48 V ชุดเก่าถูกย้ายออกจาก `output` และเปลี่ยนนามสกุลเป็น `.DO_NOT_FLASH` เพื่อเก็บประวัติเท่านั้น

ไฟล์สำหรับบอร์ดจริงอยู่ที่ `output/bldc_lite_24/bldc_lite_24.bin`
